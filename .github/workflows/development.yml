name: Deploy to Dev

on:
  push:
    branches:
      - development

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Deploy to AWS (Dev)
      env:
        AWS_ACCESS_KEY_ID: ewqe
        AWS_SECRET_ACCESS_KEY: fdsf
        AWS_DEFAULT_REGION: 'us-east-1'
      run: |
        # Add your AWS deployment commands for development here